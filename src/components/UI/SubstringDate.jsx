import styled from "@emotion/styled";
import { Typography } from "@mui/material";

const SubstringDate = styled(Typography)({
    color: "hsl(259, 100%, 65%)",
    letterSpacing: "-2px",
    marginRight: "8px",
});

export default SubstringDate;
